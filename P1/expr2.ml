(*Autor: Armando Martínez Noya
  Log in: a.mnoya
  Grupo de practicas 2.4*)

(*Un valor u de tipo int a partir de una expresión que contenga, al menos, 4 operadores
infijos.*)
let u = 9-1+2*3/1;;

(*Un valor v de tipo float a partir de una expresi´on que incluya una funci´on
predefinida.*)
let v = asin 1.;;

(*Un valor w de tipo char a partir de una expresi´on que incluya una sub-expresi´on de
tipo int.*)
let w = char_of_int 2;;

(*• Un valor x de tipo bool a partir de una expresi´on que incluya una o m´as funciones
u operadores.*)
let x = 2<3;;

(*• Un valor y de tipo string a partir de una expresi´on que contenga una frase
if-then-else.*)
let y = if 2<1 then "Armando" else "Noya";;

